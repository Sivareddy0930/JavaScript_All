// keys method is used to find the keys of  an object.
//values method is used to to find the all values of an object.

let emp={
    name:"siva",
    id:101,
    salary:80000,
    company:"Infosys",
    location:"Hydrabad",
}
console.log(Object.keys(emp));
console.log(Object.values(emp));
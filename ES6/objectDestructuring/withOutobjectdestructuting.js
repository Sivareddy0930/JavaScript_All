// if we want to store object properties to variables we can do like this.

let emp={
    id:1,
    Name:"siva",
    sal:50000
};
console.log(emp.Name);
let name=emp.Name;
console.log(name);
// but in ES6 it has been changed.
